const ROLES = {
  EMPLOYEE: 'Employee',
  MANAGER: 'Manager',
};

const LEAVE_STATUS = {
  PENDING: 'Pending',
  APPROVED: 'Approved',
  REJECTED: 'Rejected',
};

const GENDER = {
  MALE: 'Male',
  FEMALE: 'Female',
  OTHER: 'Other',
};

module.exports = { ROLES, LEAVE_STATUS, GENDER };
